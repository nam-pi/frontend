import { createBrowserHistory } from "history";

export const APP_NAME = process.env.REACT_APP_NAME || "Nampi";

export const HISTORY = createBrowserHistory();

export const SECONDARY_ITEM_LIMIT = 20;